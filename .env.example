# Copy to .env and fill in real values before running docker-compose up
ARCHIVER_DB_PASSWORD=          # PostgreSQL password for grafana_writer user
ARCHIVER_BEARER_TOKEN=         # Bearer token for API authentication
GRAFANA_ADMIN_PASSWORD=        # Grafana admin UI password
GRAFANA_ADMIN_USER=admin       # Grafana admin username (optional, defaults to admin)
