apiVersion: 1

datasources:
  - name: pscheduler-postgres
    type: postgres
    access: proxy
    isDefault: true
    uid: cf1s1g3beeccge
    url: timescaledb:5432 # Uses the Docker service name as the host
    database: perfsonar
    user: grafana_writer
    secureJsonData:
      password: $ARCHIVER_DB_PASSWORD
    jsonData:
      sslmode: disable
      timescaledb: true
    version: 1
    editable: true
